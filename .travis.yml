language: node_js

cache:
  directories:
    - "node_modules"

script:
  cd website && npm install && npm build

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  on:
    branch: docusaurus
